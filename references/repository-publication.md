# 仓库建立与发布

这份方法用于把一个本地项目建立成 Git 仓库、准备 GitHub 主页并在明确授权后发布。它把本地整理、Git 提交和 GitHub 写入分成独立权限，避免“初始化”被误解成“已经公开”。

## 1. 先冻结目标和停止位置

开始前用一句话写清：

```text
本地目录：
当前是否为 Git 仓库：
目标 GitHub 所有者与仓库名：
目标可见性：
目标默认分支：
本次允许的动作：
本次明确不做的动作：
停止位置：
```

“先给本地预览”是明确的停止条件：可以修改用户要求的仓库文件并生成预览，但不创建提交、不创建远程仓库、不推送、不改变可见性。用户之后明确要求推送时，从已经验收的本地状态继续，不重做内容设计。

上层 `SKILL.md` 可以在用户确认行为方案时，把现有远端仓库的精确提交、推送和远端验收列入同一个默认完成链。此时确认已经覆盖这些动作，不在本地验证后再次请求同一发布授权；若用户明确要求只改本地，则停止在本地验证。默认完成链仍只处理确认方案中的获准依赖闭包：创建远端、改变可见性、删除、改写历史、强制推送和无关工作树变化不随之获得授权。

如果推送请求承接同一对话中刚完成且已经验证的本地改动，先检查证据新鲜度：验收后工作树是否又变化、准备提交的差异是否仍是同一范围、当前分支与远端是否分叉、原验证是否覆盖最终提交内容。证据仍新鲜时复用已有验证，不重新运行整套开发流程；出现新增改动、冲突、生成物变化或验证结果已过时时，只重跑受影响检查和必要用户链。推送授权只覆盖核对后的准确范围，不追溯授权此前产生项目修改，也不自动包含同一工作树中的无关改动。

### 本地提交与远端状态分层交付

提交、推送、创建远端、部署或打包前，先说明本次动作会改变哪一层以及明确不会改变哪一层。当前分支已经领先远端而本轮还要创建新提交时，动作前说明未推送提交会从多少变为多少、已有提交与新提交分别服务哪个获准结果；本次推送会把已有未推送提交一并带到远端时，先列出准确提交集合并确认它们都属于获准发布范围，不能只用最后一个提交概括远端将得到的内容。

每次状态改变后从权威 Git 状态取得并按固定顺序交付：

```text
项目工作结果：
工作区与索引：
本地分支、本轮新增提交及各自用途：
相对上游的 ahead / behind：
远端分支与 HEAD 的实际核对：
尚未执行或尚未验证的推送、打包、部署与远端检查：
```

“产品修改完成”“已提交到本地分支”“已推送到远端分支”和“远端结果已经验证”分别成立。仍有一层未执行或未验证时，先给准确的分层结论，不能先笼统宣称“都做完了”再在后面附加例外。用户只授权提交时明确说明远端没有变化；用户明确授权推送时，也要在完成后重新取得远端 HEAD，并说明本地与远端是否一致。

### 跨项目根的发布状态分别成立

同一个用户结果涉及两个以上准确项目根时，先消费改动前预防形成的跨根交付账本，再开始任何暂存动作。逐根核对目录身份、职责、本轮变化、正式验证器及其实际输入、是否直接存在 Git 仓库、当前分支、远端和用户授予的发布动作。普通目录、Git 仓库、已提交、已推送和远端已验证是各根自己的状态；一个根的仓库能力、提交或远端 HEAD 不能替另一个根成立。

发布候选只属于当前获准发布的准确项目根。其它项目根提供了正式合同、同步输入或互操作依赖，只证明技术关系存在，不能自动让那个根的文件获得暂存、提交或推送授权；也不能把项目根 A 已推送、项目根 B 仍是普通目录或只有本地修改，概括成“整个跨项目结果已经推送”。用户说“推送到 main”时，必须先把准确目标根与它的候选范围对应起来；存在两个以上可能目标且历史没有唯一指向时，在暂存前说明各根状态并取得准确目标。

跨根结果的交付状态使用同一份独立矩阵：

```text
准确项目根：
本轮职责与获准结果：
本地变化和正式验证：
跨项目互操作证据：
是否为 Git 仓库及准确仓库根：
候选、提交和分支：
推送与远端验证：
仍只存在于本机或尚未验证的结果：
```

共享合同消费者若承诺在干净克隆中构建、测试或运行，仓库必须跟踪可重建该承诺的同步快照、来源身份和同步入口，或者明确声明并验证可取得的外部依赖。正式消费者测试不能静默读取相邻项目的绝对路径；这类路径只允许出现在显式跨项目集成测试的输入中，并且不能用该测试在当前开发机通过，替代当前仓库自己的干净克隆合同。

### 混合工作树先求获准依赖闭包

工作树同时包含本次结果、用户原有修改、其它任务、生成物、归档或删除时，发布候选从本次获准的用户结果开始，而不是从“当前所有能一起通过测试的文件”开始。先把已获准路径和内容片段映射到正式消费者，再沿导入、schema、构建、运行和迁移关系求出让该结果独立成立的最小依赖闭包；每项依赖同时记录来源、职责和授权状态。依赖对于编译或运行是必需的，只证明它属于技术闭包，不会让用户原有或来源不明的改动自动获得暂存与发布授权；完整工作树一起通过测试也只能证明组合兼容，不能证明组合范围已经获准。

闭包只包含来源明确且已经获准的内容时，可以按准确路径或内容片段建立候选并验证。闭包一旦需要未获准改动、无法归因的文件、受单独删除规则约束的退出路径，或必须把其它独立用户结果一起发布，立即在暂存前停止，向用户列出准确对象、依赖原因、现有验证和扩大后的远端结果，由用户决定扩大范围、另行拆分还是保持本地。不能把整个脏工作树改称“当前整合状态”来绕过确认，也不能因为拆分困难就执行 `git add -A`。只有用户看到准确清单后明确授权整个工作树，并且项目的删除、归档和发布规则分别满足时，全部纳入才成为合法候选。

在运行最终检查前，为最终候选内容建立验证账本：

```text
候选身份：工作树基线、待暂存路径及内容身份
范围归属：每项变化服务哪个获准结果、来源是谁、是否已经获得发布授权
依赖闭包：候选独立成立需要哪些输入，哪些未获准或未知依赖会阻塞暂存
变化事实：每类源码、配置、媒体、测试、文档、生成物或删除改变了什么
正式消费者：构建器、运行时、用户界面、维护者、远端检查或部署分别消费什么
检查覆盖：每项检查预期读取什么，实际选择了哪些路径、内容身份、过滤和排除项
新鲜证据：最后一次相关变化之后，哪项检查或真实用户链验证了该消费者
失效条件：哪些后续修改、生成动作、冲突或外部变化会使证据失效
```

验证强度由变化实际影响的消费者决定，不能只挑最快或最容易通过的命令。可见界面、媒体或交互发生变化时，需要从最终候选对应的真实入口经过正式状态和资源边界到可观察结果；类型检查或构建成功不能代替这条用户链。只改文档、许可证或仓库元数据时也不机械重跑无关产品回归，但必须验证相应渲染、识别或维护入口。最后一次相关变化发生在旧验证之后时，只使受影响证据失效，并在暂存或提交前补齐。

本地候选在提交或推送前验证失败时，立即冻结当前发布候选。用户只说“提交”“推送”或“推到 main”，只授权发布已经核对且达到发布条件的现有改动，不授权为了消除失败而修改源码、配置、测试、工作流或生成逻辑。只有原请求明确包含“持续修到可以发布”这类修复结果，或用户看到失败证据后另行授权，才进入修复。没有修复授权时，保留准确命令、输入身份、退出状态和失败产物，说明候选尚未达到发布条件并停止；不暂存、不提交、不推送。

得到修复授权后，继续消费原验证账本，不把一次修复当成全部证据清零。只让修复实际触及的代码、配置、测试基础设施、生成物及其正式消费者证据失效；仍与最终候选一致的新鲜通过结果继续有效。优先重跑原失败检查、受修复影响的消费者和必要用户链。只有修复改变测试收集、共享夹具、全局配置、公共边界，或者项目规则明确要求完整回归时，才重跑整套检查；不能为了得到一份整齐的新日志而丢弃仍然有效的昂贵证据。

开始前还要读取项目规则对删除、归档、生成物和提交的额外限制。若项目规则要求删除前单独征求同意，通用的“提交”“推送到 main”或“发布”不能代替删除授权；在暂存、提交或推送前准确列出所有待删除路径及理由，未获同意的删除从本次范围移除，或按用户已同意的归档方式处理。不能先提交删除再把远程发布当成用户已经接受。

## 2. 用干净克隆确定仓库边界

用户要让另一台机器、服务器或新环境从仓库直接构建、部署或运行同一结果时，先建立干净克隆交付合同，再判断哪些文件应进入仓库：

```text
仓库承诺交付的用户结果：
新环境从哪个提交和入口开始：
允许依赖的外部服务与环境变量：
安装、构建、启动或部署命令：
正式运行根、基础路径和平台条件：
最终应看见或取得什么：
本轮能够真实验证到哪一层：
```

随后按正式消费者分类，而不是按文件扩展名、当前差异或个人习惯分类：

1. **必须跟踪的输入**：源码、锁文件、构建与部署配置、迁移或 schema、运行期静态资源，以及仓库明确承诺提供的 Skill、模板、文档或维护能力。某项能力是否在当前任务中被调用，不能决定它是否属于仓库产品。
2. **可重新生成的输出**：构建目录、缓存、测试报告和本地预览产物通常由已跟踪输入生成；仓库若明确承诺预构建发布物，则它转为正式交付物，并需要来源、新旧判定和消费者验证。
3. **本地原始材料与归档**：只有在构建、运行、维护或仓库公开承诺会正式消费时才进入交付；“以后可能有用”不能代替消费者。
4. **机器状态与外部依赖**：凭据、个人缓存、已安装工具和本机配置不伪装成仓库内容；需要的版本、变量与服务通过项目正式配置和文档表达。

已被 Git 跟踪、尚未跟踪、命中忽略规则或当前页面能够从开发机读取，都只是现状信号，不能单独证明应纳入或排除。运行素材即使是二进制文件，也可能是构建或网站的必需输入；源码目录即使很重要，也可能只是不会进入发布结果的本地原始材料。

在用户允许实际运行验证时，从不继承当前脏工作树、缓存和依赖目录的新鲜检出开始，使用项目锁定的安装入口完成构建，再从正式静态根、受支持的基础路径或真实启动入口交给最终消费者。干净检出、构建成功、服务启动和用户结果分别记录；其中任一层没有运行就标为未验证，不能用当前工作区的成功拼成干净克隆成功。

## 3. 初始化本地仓库

1. 读取目录内的仓库规则、已有文件和未提交状态。
2. 用 `git rev-parse --show-toplevel` 判断真实 Git 边界。目录已经位于父级工作树内时，不创建嵌套 `.git`；先说明真实边界。
3. 只有用户明确要求初始化时才运行 `git init`。初始化不自动添加全部文件，也不自动创建提交。
4. 检查可能包含凭据、构建缓存、下载产物或本机配置的文件，依据项目事实补充 `.gitignore`；首次提交前按项目实际文本与二进制类型建立 `.gitattributes`，避免换行和生成属性由提交者机器偶然决定；不凭猜测删除文件。
5. 保留用户现有修改。提交前展示即将纳入的准确文件，不把无关改动混入。

## 4. 本地主页预览

README 与素材按主 Skill 的 README 路径制作。预览至少证明：

- README 的图片和链接能从仓库相对路径解析；
- 首屏在 GitHub 常见宽度和窄屏可读；
- 文案只陈述仓库能够证明的功能；
- 许可证链接指向唯一范围说明；
- 预览使用的正是准备提交的文件，而不是另做一份样稿。

达到“本地预览”停止位置后，给出文件差异与预览入口，等待用户决定是否继续发布。

## 5. 创建提交、远程仓库与推送

开始发布事务时，保存本次获准路径、各路径当前身份、候选提交树、对应验证和远端 HEAD。验证后以及覆盖、恢复、暂存、提交前都重新读取受影响路径；同步客户端、生成器、编辑器监视器、其它 Agent 或进程产生的变化，只有在来源、职责和当前结果已经确认后才能纳入。不能解释的变化不得用 `git restore` 消掉，也不得顺手暂存；只停止与这些路径冲突的发布动作。

候选内容改变时，只使验证账本中受影响的证据失效。完成最终验证后，暂存索引必须对应同一候选内容，并重新核对索引中的变化事实、正式消费者和新鲜证据仍然匹配；提交后再核对提交树、剩余已跟踪变化和新出现的未跟踪文件。提交之后出现的工作树变化不会自动改变已创建提交，但必须判断它是否证明提交遗漏了正式交付物：无关变化可以保留并报告，相关或未知变化在推送前停止。

创建提交、创建远程、选择或改变可见性和推送是相互独立的动作：

1. 用户授权提交后，核对作者配置、分支、工作区、当前基线和准确删除清单，再只暂存获准且来源明确的内容。任何受项目规则约束但尚未单独获准的删除都必须在暂存前停止。审计对象是 Git 实际索引，不是工作区印象：检查暂存文件清单、状态、统计和完整差异，运行 whitespace、密钥、许可证、超大文件、依赖或项目规定的检查，并在提交后核对提交树与已批准暂存集一致。
2. 用户授权创建 GitHub 仓库后，确认所有者、准确仓库名和可见性。未明确可见性时停止，不默认为 public 或 private。
3. 远程已存在时读取其默认分支和 HEAD，不用本地猜测覆盖。
4. 新远程没有提交时，使用用户当前约定或本机初始化得到的分支；只有用户明确指定时才改名为 `main`、`master` 或其它名称。
5. 远程已有提交时，不用强制推送建立表面一致；先处理真实分叉。

每条检查命令必须单独确认执行成功并立即保留退出状态，不能让后续命令覆盖失败。密钥扫描器成功运行后零匹配才是零发现；测试数据使用与真实凭据明显不同的假值，除非当前任务本来就在验证检测器。依赖审计若因镜像或接口不支持而失败，结论是未知；可以在不持久修改用户配置的前提下使用项目官方审计端点复核，不能把审计未运行说成零漏洞。

Git 仓库中的已跟踪工作树差异、暂存索引、未跟踪文件和被忽略路径是不同的工具输入集。先以候选账本确定哪些对象必须受检，再核对所选命令实际读取哪一组。`git diff --check` 只能检查它实际选中的 Git 差异；仓库没有首个提交、候选全是未跟踪文件，或者命令的差异集为空时，即使退出成功也不能证明候选内容通过。未获暂存授权时，直接对已批准候选路径执行等价只读检查；获得授权并暂存后，再让索引检查消费同一候选。被忽略路径是否应进入候选仍由干净克隆交付合同决定，不由工具默认排除代替。

远端不存在时，推送请求不会自动授权创建仓库，也不会给出可见性。先用只读身份和所有者权限检查确认缺口；创建远端、选择 public 或 private、设置默认分支、推送以及之后改变可见性仍分别需要明确授权。不能根据同一账号的其它仓库可见性替用户推断新仓库。

### 有意重建为单一根提交

只有用户明确要求把准确仓库和分支的历史替换为一个新的首次提交时，才进入单根提交事务。普通的“提交”“推送”或含糊的“继续”不授权改写历史；创建或改变远端、选择可见性、归档旧仓库、删除文件与强制更新远端也各自需要原任务已经明确覆盖或另行授权。

事务开始前建立不可省略的账本：

- 准确的本地仓库、远端所有者、仓库名、目标分支和仓库可见性；
- 已批准候选树的内容身份，以及针对该树完成的验证；
- 当前 HEAD、旧本地分支引用、已观测远端分支 SHA、默认分支和远端 HEAD；
- 旧历史备份的位置、内容身份和可恢复性；
- 预期根提交的主题、作者、树对象、父提交为空和提交数为 1；
- 精确租约条件、远端变化时的中止条件和推送后的远端检查。

在改写本地引用前，按已获授权的方式保留旧 `.git`、引用或等价历史备份，并实际验证备份能够识别原 HEAD；没有删除授权时不得为了得到“干净的新仓库”删除旧历史。新根提交只能从已经批准和验证的候选索引或树创建，创建后核对其树对象与候选完全一致、父提交为空、目标分支提交数为 1。更新本地分支引用时使用预期旧值作为条件，当前引用变化就中止，不能覆盖来源不明的并发修改。

远端更新前重新读取目标分支。历史替换使用精确的 `--force-with-lease=<branch>:<observed-sha>` 或语义等价的条件更新，不使用裸 `--force`；若远端不存在时的预期值与远端已有提交不一致，或已观测远端分支 SHA 之后远端发生变化，立即中止并重新确认，不能扩大租约。

推送后必须重新获取远端，并核对仓库可见性、默认分支、远端 HEAD、根提交树、父提交为空、提交数为 1、禁止路径或应归档内容未进入新树，以及当前用户结果依赖的远端检查已经完成。若验证或远端检查修复使最终候选发生变化，应以新候选重新验证并再创建一个无父提交的根，而不是在它后面追加修复提交；再次更新远端时也必须使用新读取的精确租约。仅有“推送成功”不能证明单根历史替换完成。

## 6. 发布验证

推送完成不等于发布已经可用。沿最终用户链路验证：

1. GitHub 仓库的所有者、名称与可见性正确。
2. 默认分支是预期分支，远程 HEAD 指向刚发布的提交。
3. README、许可证和关键素材在该提交中存在，Git blob 与本地提交一致。
4. GitHub 仓库首页能够读取 README，图片、锚点和链接可用。
5. GitHub 的许可证识别结果与范围设计一致；混合许可证显示 `Other` 不自动视为失败。
6. 当前用户结果依赖的 required checks、GitHub Actions、部署任务或其它远端验证已经结束并达到正式成功状态；与当前结果无关的可选检查单独报告。
7. 用户要求可部署或可运行时，从远端准确提交取得的干净克隆仍能完成交付合同，并由真实入口形成最终用户结果；本地工作树、远端提交存在和部署成功是不同证据。

跨项目结果在上述检查后仍逐根保留结论。只有账本中的每个目标根都取得相应提交、推送和远端证据时，才能说所有目标根已经发布；互操作通过但其中一个根未入库、未提交或未推送时，应分别报告“跨项目链已通过”和该根尚未发布，不能把两项拼成更高层结论。

验证失败时说明失败位于本地提交、干净克隆、安装或构建、传输、远程分支、远端检查、部署、GitHub 渲染还是识别层。读取状态、日志和失败产物属于只读发布验证；修改源码、配置、工作流或远端设置来修复失败，只有原请求已经明确要求持续修到发布可用，或用户另行授权时才执行。未经授权不通过强制推送、删除远程或改可见性来“修好”。
