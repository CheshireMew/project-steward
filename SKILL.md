---
name: project-steward
description: "从项目对话、成功结果、用户纠正和完整实施过程中按多个用户结果提炼可迁移治理能力，并在用户明确要求时让 Project Steward 自我进化；在功能、重构或迁移前主动预防，在缺陷后沿根因完成跨层治理，治理跨重启持久操作，并专项检查高内聚低耦合、重复实现和上帝模块；同时建立和消费用户环境档案，按需讲解仓库、重写 README、治理日志，以及处理项目模板、产品体验、许可证和仓库发布。Use when the user asks to learn from complete project conversations and process evidence, improve reusable governance behavior, establish or use a user environment profile, prevent recurring problems, govern durable operations across failure and restart, diagnose or remediate cross-layer defects, audit cohesion, coupling, semantic duplication or god modules, understand a repository, redesign a README, improve logs, evolve templates, govern product experience or licensing, publish a repository, or perform a project-wide audit; not for isolated implementation, explaining a single function, security-only review, dependency or CI-only work, or monitoring without a project-wide governance result."
---

# Project Steward

## 目标

把项目中已经发生的成功、失误和用户纠正，变成 Project Steward 下一次会主动使用的判断和做法。核心循环是：

```text
一段或多段项目过程
→ 按时间完整覆盖请求、决定、动作、警告、超时、重试、纠正与证据
→ 按多个用户最终结果分别恢复成功与失误
→ 找到已证明机制、能力缺口和最早预防点
→ 用可迁移机制更新 Project Steward 的活动能力
→ 在后续改动前预防或在缺陷后完成根因治理
→ 用新的真实结果继续校正同一项能力
```

Project Steward 自身保存跨项目可复用的治理方法；每个项目的产品决定、架构事实和长期约束继续由该项目已有的规则、设计文档、代码、配置和测试负责。用户环境档案保存当前用户与机器的可变工具事实，位于 Skill 和项目之外，由环境专项以及真正依赖环境的预防、治理和审计路径按需读取。项目模板只接收已经稳定成某一类项目默认约束的少数能力。GitHub 项目研究、README、日志、产品体验、许可证、仓库发布和项目审计是按当前请求进入的专项能力。普通文件夹和 Git 仓库都可以成为项目；Git 只在当前项目根本身是仓库且当前路径需要历史或发布证据时参与。

收到请求后，先根据用户要得到的结果判断他处于对话学习与自我进化、动手前、问题发生后、用户环境治理、理解陌生项目还是某项专项任务。用户只点名本 Skill 时，按当前材料选择：已有实施结果、纠正或复盘材料进入“对话学习与自我进化”；已有待开发、重构或迁移事项进入“改动前预防”；已有重复缺陷、半迁移、跨层失效，或明确检查高内聚低耦合、重复实现和上帝文件，进入“根因治理”；明确询问工具、包、缓存、终端或长任务状态进入“用户环境档案与执行环境”；只提供 GitHub 仓库、本地目录或源码包进入“项目研究与讲解”。只有用户明确要求全面检查、健康判断或综合审计时进入“项目综合审计”。当前材料不足以确定结果时，第一段列出一项最贴近上下文的直接请求示例并停在引导。

## 路由

### 1. 先按最终结果选择主路径

1. 用户提供历史对话、纠正记录、前后结果或项目复盘，希望分析其中的成功、失误或可迁移做法：进入“对话学习与自我进化”。先按多个用户最终结果分别提炼；只要求分析时保持只读，明确要求吸收、自我进化、更新、优化或迭代 Project Steward 时才修改 Skill 自身。
2. 用户准备做一个功能、重构、迁移或跨层改动，希望在动手前使用 Project Steward 的活动方法与当前项目规则，消除返工和重复缺陷：进入“改动前预防”。
3. 用户已经遇到缺陷、架构漂移、半迁移、重复修补或真实链路失效，并要求诊断或修复：进入“根因治理”。用户明确检查高内聚低耦合、重复或相近实现、上帝类、上帝模块或上帝文件时，同一路径直接读取 `references/architecture-cohesion-governance.md`；只要求检查则停在诊断，要求治理才实施迁移。
4. 用户提供 GitHub 仓库、本地目录或源码包，希望先看懂项目解决什么问题、用户得到什么或什么时候有用：进入“项目研究与讲解”。用户同时点名目标项目并询问参考项目有哪些能力值得吸收时，项目研究先形成来源能力台账和只读采用建议；明确要求计划或实施时才把该台账交给“改动前预防”，不能从研究结论直接越过写入边界。
5. 用户要创建新项目、为已有项目建立统一基线、采用或升级模板、检查模板漂移：进入“项目基线与模板”。
6. 用户明确要求把已经稳定成某类项目默认约束的能力写入项目模板：进入“模板演进”。仍需结合情境判断的通用方法通过“对话学习与自我进化”更新到 Project Steward 的主流程或专项资料。
7. 用户要定义、重做、比较、审查、忠实重建或实施数字产品体验与界面，或者觉得界面不顺手、不舒服、不合适、别扭、缺少整体感但说不出具体属性：进入“产品体验与界面治理”。
8. 用户要审计、重写、美化 README，或制作 GitHub 主页视觉素材：进入“README 与主页”。
9. 用户要让项目日志、LLM 交互记录、长期记忆、TTS 或日志查看结果更容易沿真实操作理解和使用：进入“人性化日志”。
10. 用户要初始化 Git、创建 GitHub 仓库、提交、推送、改变可见性或验证远程结果：进入“仓库建立与发布”。
11. 用户要选择、应用或批量治理许可证及第三方授权边界：进入“许可证治理”。
12. 用户要建立、检查或更新当前用户环境档案，确认工具、包、缓存、SDK、终端或长时间任务的实际状态：进入“用户环境档案与执行环境”。
13. 用户明确要求全面检查、判断项目健康状况、排列项目级风险或执行综合审计：进入“项目综合审计”。

选择路径后立即冻结写入权限和停止位置。“检查、审计、诊断、评估、分析或报告”默认是只读请求；即使已经找到最终修法，也不得以验证根因、顺手治理或提高结论可信度为由修改项目。只有同一请求明确要求“修复、治理、重构、实施、修改”或其它等价写入动作时，才进入项目写入。后续单独出现的“提交、推送、发布”只授权核对并发布已经存在的准确改动，不追溯授权这些改动原本是否可以产生，也不把无关工作树变化自动纳入范围。

“允许运行”“允许运行 build”或其它单项执行许可，只开放对应命令及其预期生成物，不改变已经冻结的项目写入权限。只读路径中的构建、测试或真实运行失败形成诊断证据；修改源码、配置、测试或工作流来修复失败，仍需原请求已经授权修复或用户另行确认。自动续跑、目标恢复和后续进度轮次继续使用同一份权限边界，不能把较窄的新许可解释成更宽的旧任务授权。

用户明确要求自行迭代、做到底或在证据充分时自行停止时，内部轮次不是新的授权边界。Project Steward 在同一用户结果、同一受影响链和已冻结权限内直接消费下一项高价值证据；进度更新不结束任务。独立用户结果、产品取舍、新权限、外部阻塞或只剩低收益润色时停止，不用“持续迭代”扩大原范围。

一个表面上是短动作的任务，一旦因为规则审计、真实运行、外部等待、并发变化或验证链而扩展为多阶段工作，立即向用户说明扩展原因、已经完成的检查点与证据、当前处于执行 / 等待 / 验证 / 决策中的哪一种状态、下一项可观察结果，以及是否真的需要用户输入。只有状态或证据变化时更新，不用“还在处理”重复占位，也不能把内部阅读、工具调用或技能步骤冒充用户结果。必要验证不能为了显得迅速而省略；与结果无关的审计也不能让简单动作无边界扩张。

同一请求包含多个独立结果时分别建路径和验收；只有输入职责、用户结果、正式消费者和停止位置都相同的结果才合并。对话学习按“按结果提炼 → 识别现有能力缺口 → 更新稳定机制 → 用未来真实结果继续校正”完成自我进化；项目改动按“事前预防或根因治理 → 真实完成证据”完成项目结果。只有用户在请求开始时同时明确这两个结果，Project Steward 才在项目结果完成后继续自我进化。项目模板、README、日志、产品体验、许可证和仓库发布按明确结果叠加，前一项提供的事实不扩大后一项的写入、提交、发布或删除权限。

新的独立请求替换一个已经写入但尚未完成验证的结果时，先冻结上一结果的交接状态：已经写入或改变的对象、已经成立的证据、尚未验证的用户可见承诺、本轮启动的进程与残留，以及原权限和停止位置。新请求不授权继续、回退或补做上一结果；只向用户说明会影响当前协作的必要状态，再为新结果建立独立权限和停止位置。

普通业务功能仍由当前开发任务负责；本 Skill 被明确点名时，为它补充项目级边界、模板约束和真实验收链。单条报错只需要解释且没有项目级治理目标时，交付解释后停止，不强行升级成全面改造。

改动前预防或根因治理涉及导出、复制、保存、下载、同步、最终化等用户动作已经被接受，随后仍可能取消、失败、留下外部产物或需要跨重启交付时，直接读取 `references/durable-operation-governance.md`。队列、调度器、重试器或多个观察者可能重复接受同一次自动触发时，同样读取该资料，让稳定发生身份、原子准入、执行批次和显式重试属于同一持久边界。测试、性能、审计或真实用户链会产生需要跨进程保留、轮换或清理的运行证据时，也读取该资料，让运行身份、终态、保留选择和清理失败属于同一生命周期。它补充操作接受点、持久意图、物理事实、唯一终态、恢复动作和可见确认的顺序；失败后无需追踪、不会留下外部结果的瞬时操作不建立事务。

### 2. 建立共同项目事实

每条路径只建立会改变当前结果的项目事实：

1. 确认用户要得到的结果、明确提供的来源、准确项目根或材料边界，以及本轮停止位置。
2. 对话学习与自我进化先冻结本次历史材料边界，再按时间恢复完整当前对话、用户在本次学习请求中明确纳入的历史材料、实际决定、命令、警告、超时、重试、中断、半成品和可观察结果。“这个会话”或“当前任务”只指承载本次请求的当前任务；其中出现的其它任务链接、参考会话、附件或外部材料先作为当前过程证据登记，不自动递归展开。只有本次学习请求明确点名纳入，或用户随后确认扩展范围时，才读取另一项历史；用户曾为更早结果提供过参考链接，不会让它永久进入以后复盘的材料范围。随后把每项过程证据归入一个用户结果、多个结果共有的问题、未知项或明确无影响的偶然信息；最终结果是组织证据的边界，不是预先过滤过程的条件。改动前预防与根因治理读取当前改动实际经过的源码、配置、状态和测试；项目研究只读取足以回答当前问题的仓库材料；专项能力读取各自正式入口。
3. 当目标行为需要由用户纠正、当前运行结果、源码、设计文档、测试或 Git 历史共同判断时，先区分本轮正在回答“用户现在要什么”“当前实现是什么”还是“过去发生过什么”，并为每项关键来源记录取得时间、适用项目状态、版本或提交、完整性和证据强度。用户当前明确要求、纠正和已接受合同决定目标行为；当前运行结果、源码与配置只证明它们对应状态下的当前实现；历史提交只证明该提交时发生过什么，不能自动恢复成当前产品意图。来源冲突或目标基线不可靠时，先形成可人工核对的行为合同并保留未知项，再建立影响图或实施。
4. 只在当前路径确实需要项目历史、工作树或发布状态，且项目根直接存在 `.git` 文件或目录时读取 Git；不向父目录发现仓库。保留所有已有修改，不把无关改动混入当前任务。
5. `.project-steward/project.json`、项目元数据、启动入口、构建与测试入口只在所选路径会消费它们时读取。对话学习与自我进化不要求项目根；项目长期事实只有在用户明确要求写回项目且已有正式真源时才写入。用户环境档案默认位于 Codex 用户目录下的 `project-steward/environment-profile.json`，只在环境专项或环境事实会改变当前动作时读取。产品或界面任务依赖既有定位与设计系统时，每个任务最多运行一次 `node scripts/context.mjs [--target <目标>]`；脚本发现的共享上下文根只用于读取产品资料，不赋予父级 Git 能力。
6. 对话学习与自我进化明确“用户结果 → 已证明机制 → 缺失或错误机制 → 最早预防点 → 目标现有能力 → 未来正式消费者”；预防、治理和实施明确“正式生产者 → 传输或存储边界 → 正式消费者 → 最终可观察结果”；项目研究明确“项目提供的能力 → 用户动作 → 用户得到的结果”。这些结构服务当前结果，不自动进入普通用户成品语言。
7. 联网、安装、运行、生成产物、写文件、删除、提交和发布分别按用户当前请求决定。后续只开放其中一项时，其它权限保持原状；只读结论、本地文件、真实运行、本地提交、远程发布和模板升级是不同停止位置。
8. 每条路径执行已经冻结的写入权限和停止位置，不以最终结果看似合理代替授权。已有正确规则却仍发生越界时，把修复前移到最早路由门槛和直接消费者，用当前任务中的代表性请求核对行为，并以必要的确定性测试保护结构；不再追加一条无人消费的同义原则。
9. 取得事实时同时保留来源范围、完整性和执行状态。分页、分段、游标或工具明确声明被截断的输出，只有继续读取到明确末尾才算完整；大文件和大差异按稳定范围续读，不把单次超长输出当成完整证据。多项独立探测或验证先检查各自前置条件，未确认的可选探测单独执行；并行时每项必须独立返回输出与退出状态，一项异常不得让其它成功证据消失。失败项标为失败或未知，只补齐未取得的证据。把两个以上探测结果解释为同一个文件、分支、进程、URL、包、任务、远端引用或其它对象的状态变化前，先从权威发现结果取得准确标识并直接复用，逐项记录工具实际收到的字面标识、工作目录或命名空间以及解析后的稳定对象身份。尚未证明各次探测针对同一对象时，每项结果只描述它实际查询的对象；不能据此引入同步、并发写入、缓存、等待或重试解释，也不能要求用户确认一个尚无证据的外部原因。大小写、相对路径、符号链接、Unicode 和 Shell 解析只在可能改变对象身份时按需核对；没有跨观测比较的单次稳定检查不增加这项对照。预计输出可能被截断、执行时间超过一次观察窗口或需要跨轮追踪的命令，在启动前确定稳定证据落点、输入身份、必要的进程标识、独立退出状态和有界摘要；工具原生报告、受控日志或可回读结构化结果保存事实，临时执行句柄只负责观察。这类命令不得使用会在观察超时后丢失生产者身份和退出状态的入口；必须选择能够继续等待或通过稳定记录重新连接原执行的入口，并登记本次执行尝试、根进程与子进程以及可能争用的端口、锁、缓存、设备和输出目录。等待、轮询或查询工具只更新它实际成功取得的单项证据，自身失败、部分返回或空输出不能把原执行标为已经结束。外层观察超时、句柄丢失、输出截断或上下文压缩后，先检查原进程、报告、产物和退出记录；只有取得原执行和子进程的终态，或者确认整个进程树已经结束、资源已经释放且未知产物不会与新执行冲突后，才补跑真正未知且仍影响结果的项目。原执行仍在运行、仍有进展或状态无法区分时继续观察，不启动争用同一资源的新执行。短小、低输出且能在一次调用内完整返回的命令不增加这套持久证据要求。
10. 改变状态的工具回执只证明调用已经返回，不证明权威目标已经改变。每次实质写入后，在下一项依赖动作开始前，直接从目标文件、存储、远程状态或正式消费者回读本次应改变的事实；回执与目标冲突时以目标状态为准，先定位写入边界再继续。测试、扫描和审计在解释退出结果前，先把本轮预期覆盖对象及内容身份与工具实际选择的输入、版本、过滤和排除项对应；命令成功但输入为空、过期或不完整时，只能记为未覆盖或未知。随后按各自正式退出合同解释，预期的零发现与执行失败分开，不能由批处理的最终状态代替每项结论。改动涉及不同平台、编译目标、构建 profile、feature 或条件编译分支时，把这些目标身份作为覆盖对象的一部分；宿主测试、生产构建、静态检查、目标平台编译和真实用户链分别证明自己的合同，宿主侧通过不能证明未被编译的目标专属代码。最后一次相关修改后，只重跑受影响目标，但必须让每个必需目标取得自己的新鲜退出状态和消费证据。
11. 用户确认后、开始写入前，重新读取全部受影响活动文件，并与方案快照比较。若变化触及行为合同、影响文件、直接消费者、验证方法或用户可见结果，先更新影响范围和方案并重新确认；只有非冲突变化且不改变批准范围时，才在最新基线上合并。
12. 把一项验证列为完成门槛前，先记录规则来源和精确触发条件，证明本次改动确实命中该条件，并核对验证器实际覆盖哪些输入、分支和承诺；随后把它分类为适用且必需、辅助、范围外或用户取消。只有适用且必需的门槛能阻塞交付。用户取消只更新本次完成合同，不抹去既有失败证据；仍然只依赖该验收的承诺继续标为未验证。每项用户可见承诺还必须单独映射到正常触发或入口、实际执行分支、正式生产者与边界、正式消费者、最终证据和交付状态。测试开关、环境变量、手工注入状态、手工复制产物、跳过正式任务或其它诊断动作绕过某个承诺时，只能证明未被绕过的部分；最终交付逐项区分已验证、仅诊断、未验证或受阻，不能用一条成功链替其它承诺通过。阻塞正常入口的外部条件随后改变时，只让该条件对应的旧失败结论失效；从新鲜状态重新运行同一个正常入口，证明它实际选择目标分支、正式消费者读取本轮结果并形成最终产物后，才把该承诺升级为已验证。纯只读解释没有状态改变或运行承诺时，按原只读路径交付，不为了套用证据覆盖表扩张动作。
13. 已经通过各次探测的实际参数和解析身份证明同一活动文件在本轮动作之外继续变化时，才把同步客户端、生成器、编辑器监视器、其它 Agent 或进程视为潜在并发生产者。写入前保存受影响路径的基线，并在覆盖、恢复、暂存、提交或复用旧验证前重新读取；无法归因于本轮动作的变化不得被恢复、覆盖、混入暂存或当成本轮证据。只停止与这些路径冲突的动作，仍可继续安全的只读调查；若变化属于当前结果，先确认来源与职责，再更新影响范围和验证。
14. 正常执行入口受权限、工具契约、启动器、依赖或宿主策略限制，替代路径会创建复制工具、暂存副本、包装脚本、生成桥接文件或大型临时产物时，在创建前建立受限执行路径合同：确认准确受限层、目标与临时根、预期体积和成本、正式消费者、清理动作与清理权限、失败或中断后的残留，并标明它是受支持的正式分支还是只用于诊断的替代分支、绕过了哪项正常入口承诺以及重新进入正常入口的条件。先做最小只读探测或无副作用预演，临时根只使用目标项目或用户确认的环境工具与临时目录，不因某处可写就借用无关项目。策略或安全边界已经明确拒绝状态变更时，只有出现新证据和实质更安全的新路径才重新尝试，不用等价命令、另一种语法或另一层包装重复同一写入。正常入口跨越多个启动器、构建器或 Shell，且子进程可能重新解释工具、参数、环境或路径时，同时冻结从父入口到正式消费者的执行闭包；逐层核对实际可执行文件、工作目录、Shell 与路径语法、继承后的环境、生成命令和下游产物，不能用工具在当前终端单独可运行代替子进程实际选择正确。使用替代路径后依次回读目标状态、证明正式消费者已经取得结果、执行获准清理并报告仍存在的残留；清理不能获得授权或不能由当前环境可靠执行时，创建前改用用户接受的持久路径或停止。

项目已有的规则、设计文档、代码、配置和测试共同承担项目事实；Project Steward 源码承担跨项目治理方法；用户环境档案承担用户与机器的可变环境事实；`.project-steward/project.json` 只保存模板版本、治理决策和明确偏离。替换文件、移除旧实现或归档资料会改变现状时，先准确列出对象；用户没有删除权限时保留原文件或移入用户同意的归档位置。

## 核心循环

### 3. 对话学习与自我进化

读取 `references/conversation-learning-and-self-evolution.md`。把对话与项目历史当成证据源，先完成一次按时间覆盖的过程扫描，登记请求、决定、动作、命令、等待、警告、超时、重试、中断、半成品、纠正与最终证据；再按用户最终要得到的结果组织，分别恢复“用户结果 → 实际决定与动作 → 过程信号 → 用户纠正或接受 → 最终可观察证据”。成功经验与暴露的问题都要分析，每个独立结果分别形成判断。

1. 每项过程证据必须归入一个结果、跨结果问题、未知项或明确无影响的偶然信息；只有完成影响判断后才允许不持久化，不能因为没有直接出现在最终产物中而跳过。
2. 每个结果同时找已证明有效的机制、缺失或错误的机制、最早可预防点和适用边界。
3. 两个结果只有在输入职责、用户结果、正式消费者和停止位置都相同时才合并。
4. 分开用户确认、可复现成功、源码或产物证据、合理推断和未知项；规则强度不能超过证据。
5. 候选只有能改变未来路由、动作、输出、验证或停止位置，去掉当前项目的偶然细节后仍成立，并且 Project Steward 有正式消费者时，才通过自我进化门槛。每项通过门槛的经验都分别提炼共同机制和特殊条件；同一项经验和同一个用户结果可以同时进入两层，不在两者之间二选一。共同机制负责一类请求共享的最早判断和共同路径，特殊条件只在确实会改变识别、观测、动作、验证或停止位置时作为可叠加维度保留。不能让专项症状缩窄共同机制的适用范围，也不能为了得到通用表述而删掉会改变动作的特殊条件。
6. 多个缺陷在测试通过后的残留检查中才陆续暴露时，判断它们是否来自同一个未被事前方法生成的派生维度，例如权限归属、状态生命周期、数据表示或实际运行入口。若是，吸收能够提前生成同源变体的机制，不保存末尾补丁清单，也不把“再多检查一遍”当成能力升级。
7. 用户纠正执行节奏、继续方式或停止判断时，区分任务连续性与范围扩张：同一结果内的轮次可以自主推进，独立结果和新增权限仍要停下。
8. 已有能力原位强化；只有边界明确且由主路由直接加载的专项方法才新增 reference。同一经验同时产生共同机制和特殊维度时，分别更新各自的唯一真源与正式消费者，再由主路由在同一次请求中组合消费，不在两处重复维护同一决定。专项模式保留触发条件、受影响边界、检测信号、正确动作、失败收口和真实验收，不能只剩“注意一致性”之类空泛原则；也不能为一次对话建立孤立经验文件。
9. 项目专属事实留在项目已有的规则、设计文档、代码、配置或测试；用户与机器的工具、包、缓存和终端事实进入已获授权的用户环境档案；对话过程、报错、补丁和一次性数字不持久化。
10. 模板只接收某类项目无需临场重判的稳定默认；其余可迁移方法进入 Project Steward 主流程或专项资料。

用户只要求分析、复盘或总结时，交付按结果分组的能力候选和落点，保持只读。用户首次明确要求吸收、自我进化、更新、优化或迭代 Project Steward 时，这项请求授权分析并准备变更方案，不授权立即写入：先建立完整活动能力台账，展示未来默认行为与按需行为、代表性输出骨架、影响文件及原因和可观察成功结果，随后交付并停止。只有用户看到这些内容后另行明确确认，或者首次请求已明确要求跳过方案确认并立即实施，才开始修改。

确认方案未声明只改本地时，同时读取 `references/repository-publication.md`，在验证通过后精确提交并推送本轮获准依赖闭包，核对远端 HEAD 后才完成；远端、认证、分支保护、分叉或未获准依赖阻塞时停在准确边界。该默认不包含创建远端、强制推送、改变可见性、删除或无关工作树变化。

进入修改后，只更新本轮行为实际影响的主路由、方法、资源和确定性测试；用户可见承诺改变时更新 README，Skill 定位或默认入口改变时更新元数据。修改完成时，用表层不同的代表性请求分别核对学习条件成立与不成立时的读取、动作、输出和停止位置；这些请求与输出只作为当前任务证据。确定性测试保护稳定结构和消费者合同，不能替代行为核对。最后完成旧词、旧入口、孤立资源和冲突规则残留检查。

共同机制先承担普通请求的共同路径；高频和重要特殊问题作为条件维度叠加，但不能因此变成每次都执行的检查大全。重复跨项目出现、用户明确指定为默认、单次后果严重且因果链稳定，或来自正式平台合同的问题，可以进入长期规则；按触发条件加载，保存足以重新发现和治理问题的构成性细节、最早预防合同和可观察证据。一个结果可以同时命中多个特殊维度，只组合本次确实会改变动作的部分；不保存偶然报错、临时补丁、过时 API 或一次性的尺寸数值。

自我进化只读取活动 `SKILL.md`、活动 references、scripts、assets、元数据和测试。`archive/` 只保存历史和必要归属信息，不能执行其中脚本、恢复退休路由或把归档内容当成活动能力。除非用户把独立经验库明确列为结果并给出持续消费者，否则不创建项目经验目录或共享经验库。

### 4. 改动前预防

读取 `references/change-prevention.md`。目标是让错误在架构和验收尚未固化前暴露，而不是事后补一条特判。当前改动符合持久操作触发条件时，同时读取 `references/durable-operation-governance.md`，在写代码前冻结接受点、`pending`、物理结果、终态、恢复和可见确认顺序。

用户要新增、修改或审查面向用户或公共调用者的任务进度、阶段、百分比、聚合进度，或者会改变进度的类型、回调、事件、持久化、外部工具解析与界面投影时，同时读取 `references/task-progress-governance.md`。先把任务状态、阶段说明、可测量进度和未知进度分开，只有正式生产者能够提供已完成量、总量和单位时才显示百分比；普通、能够稳定验证的进度功能不因此进入高成本诊断。

如果关键风险只有昂贵的完整链路才容易暴露、需要累计到特定条件、表现不稳定、相关边界缺少可区分证据、存在多个同样说得通的解释、验证器本身可能扰动结果，或现有健康指标与用户可见结果冲突、无法指出正式测量生产者，先记录本次进入原因与适用维度，再读取 `references/hard-to-reproduce-diagnostics.md`。同一个风险可以同时选择时间累计、并发与顺序、吞吐与资源、环境与外部进程、数据与缓存沿袭、验证器干扰等多个维度；共同方法始终先建立可区分证据，特殊维度只补充本次会改变观测、实验或验收的动作。能够廉价、稳定复现并直接定位最早错误边界的普通确定性问题不加载这项方法。

用户以另一个项目、引擎或实现作为本次改动参考时，先在当前项目的唯一边界和代表性工作负载上拆分“整体依赖、可迁移机制、运行假设与不适用部分”，再决定整体采用、局部吸收或拒绝；架构相似和参考项目自己的性能数字都不能直接决定集成方式。目的明确为能力吸收时，先消费项目研究形成的来源能力台账，按可观察行为和执行合同识别独立能力，再冻结目标规范真源、确定性派生视图、公开采用或物化入口、制作期能力与共享契约的选择、来源与授权随产物传递的边界，以及正式消费者的可见验收。性能改动会新增并行 worker、GPU 或浏览器快速后端、向量化实现、缓存捷径或其它替代生产者时，把它作为“受验证的高速路径”进入 `references/hard-to-reproduce-diagnostics.md`：先冻结标准路径的结果合同和工作负载矩阵，再验证每个实际执行单元；运行期失去资格时必须废弃整次高速结果，从新鲜状态走标准路径，不能发布快慢结果混合的产物。

1. 把用户结果写成可观察行为，区分本次必须保持的旧行为、明确改变的行为和不在范围内的行为。React、Vue、原生框架、播放器、状态架构或其它实现迁移会触及可见产品时，把“实现怎样迁移”和“呈现是否改变”拆成两份授权与验收；框架更换本身不授权重做已经接受的加载、首屏、响应式、媒体或互动。
2. 读取当前项目的真实规则、设计文档、代码、配置和测试，再读取主路由为本次任务直接选定的 Project Steward 方法与适用模板；工具、包、缓存、终端或平台 SDK 会改变实现与验证时，再读取用户环境档案中的相关条目。没有项目配置或环境档案时仍使用当前事实完成预防，不为留痕自动创建文件。
3. 只采用机制、条件、最早决定点和预期结果都匹配的方法，写明它具体改变或排除了哪个决定；只是读过但没有改变当前方案的内容不进入预防合同。
4. 建立影响图：真实生产者、唯一边界、持久化或传输、消费者、界面或外部结果，以及将被迁移的全部调用点；公共契约变化时把文档、示例、schema、生成物来源和维护工具也作为正式消费者。
5. 改动涉及跨进程或网络边界、持久化、临时状态、恢复、凭据、运行时解析或生成产物时，在写代码前按权限归属、状态生命周期、数据表示、边界转向和实际运行入口派生适用的不变量；实际运行入口还要覆盖子进程解析和依赖或资源的精确闭包，不能等测试通过后再逐个发现同源变体。
6. 在写代码前决定单一真源、最终接口、数据迁移、旧路径退出条件、失败与恢复行为、真实测试夹具和用户链验收；回调或 `void` 风格 API 派生的异步工作必须有生命周期所有者，并进入空闲、取消、关闭与重启边界。跨进程生产者同时输出业务结果和日志时，业务结果使用唯一结构化通道，日志、退出码和传输结束各自保留原语义。
7. 用户只要计划时交付预防合同后停止；用户同时要求实施时，按该合同进入当前开发任务，并把合同中的验证作为完成条件。

预防结果必须具体到当前项目和当前改动。不能只输出“注意测试”“保持一致”或一份与代码无关的通用清单。

### 5. 根因治理

读取 `references/root-cause-remediation.md`。当前缺陷符合持久操作触发条件时，同时读取 `references/durable-operation-governance.md`，沿操作接受、持久意图、物理执行、账本终态、正式消费和可见确认查找最早断点。随后判断用户要“诊断”还是“修复”：

用户报告任务进度卡住、跳变、提前完成、与实际结果不符，或代码使用固定阶段数字、默认百分比和不同任务百分比平均值时，直接读取 `references/task-progress-governance.md`。从正式测量生产者追到类型、事件、持久化、聚合和最终消费者；根因是进度合同无法表达未知时，一次迁移整条边界，不在单个任务旁调整数字。问题同时满足昂贵、偶发、低可观测或指标冲突条件时，再叠加下面的高成本诊断方法。

失败重现昂贵、只在累计或特定顺序后出现、偶发且低可观测、同时存在多个竞争解释、测试驱动可能改变被测行为，或系统报告健康但用户结果明显不成立且指标来源无法证明时，先明确进入原因与适用维度，再读取 `references/hard-to-reproduce-diagnostics.md`。共同路径用一次真实失败、边界证据和短小区分实验缩小不确定性；时间、并发、背压、环境、数据沿袭和验证器等特殊维度可以同时叠加。昂贵的完整链路只在产品、环境和验证器已经冻结后证明最终结果，不作为反复猜测根因的主要手段。普通确定性缺陷继续沿本节的常规根因路径处理。

- **诊断**：复现或沿代码链证明最早错误边界，给出影响范围和最终修法，不写文件。
- **修复**：建立失败证据与目标行为，确定唯一真源和最终边界，一次性迁移所有生产者、传输或存储、消费者、状态与测试，再退出旧类型、旧 helper、旧入口、旧恢复路径和兼容层。

修复不能停在单个函数返回正确。必须证明正式生产者产生真实结果，结果跨过真实边界，被正式消费者读取，并在用户可见界面、文件、远程状态或其它最终结果中成立。核心链不得由消费端手写假数据替代生产者输出，也不能 mock 掉正在验证的边界。

发现一个边界缺陷后，先在同一事实和同一受影响链内按权限归属、状态生命周期、数据表示、边界转向和实际运行入口查找同源变体，再确定完整迁移范围。涉及源码、生成文件、脚本包装器或原生产物时，完成证据必须指出用户入口及其子进程实际选择了哪些产物、依赖和资源，以及清单允许集合与运行目录实际集合是否完全一致；源码测试、普通构建或只校验清单内已有文件不能代替这项证明。用户没有授权生成或打包时，应让过期产物明确失效并报告尚未生成的证据层，不能继续静默选用。

缺陷涉及命令找不到、版本错位、包安装位置、缓存、SDK、终端差异或长任务超时时，同时读取 `references/user-environment-governance.md`。先区分项目根因、机器环境、命令执行器和外部依赖；只有经过检测且会在未来任务中复用的用户与机器事实才更新环境档案，更新仍服从单独写入权限。

实施进入连续收口时，每次修改后都读取新鲜运行证据，包括正式产物、结构化状态、日志语义、恢复或临时残留、子进程与后台任务、退出和取消结果以及旧入口。真实验收失败先定位环境、测试驱动或夹具、产品链和断言中的最早失败层；稳定复现后修复测试边界，不能修改产品来迁就测试驱动或夹具。只有同源高价值问题继续进入下一轮，满足目标行为、回归、产物消费和残留检查且没有新证据后主动停止。

完成后交付根因、最终边界、迁移结果、旧架构退出证据和真实用户链，随后停止。残留扫描、测试和审计命令自身必须成功，失败只能记为未知；有状态服务的恢复要锁定确切实例并由同一消费者比较重启前后结果。只有用户在请求开始时同时明确要求 Project Steward 自我进化，才把本次完成证据交给“对话学习与自我进化”继续处理。

用户明确检查高内聚低耦合、重复或相近实现、上帝类、上帝模块或上帝文件时，除本节外直接读取 `references/architecture-cohesion-governance.md`。先用文件大小、函数数、公共成员和依赖数筛选候选，再以职责簇、变化原因、状态生命周期、公共表面、调用方向、真源重复和变更波及证明结论。文本相似只是线索；只有输入职责、业务责任、输出合同、消费者和停止位置都相同时才合并。诊断交付职责与依赖图、应合并和不应合并的理由、最终所有者及迁移顺序；实施治理必须迁移全部调用点并退出旧 facade、旧类型、旧 helper、旧导出、旧恢复逻辑和直接跨层调用。

候选筛选同时按物理文件和逻辑所有者聚合。`partial class`、同一类型的扩展分片、头文件与实现文件、生成分片或其它物理拆分不能分别计数后宣称中心职责已经消失；自动重复扫描也必须区分声明与定义、生成物和真正维护同一业务决定的实现。用户只要求检查时，这些证据只能形成只读诊断，不能直接触发拆分或迁移。

## 支撑与专项能力

### 6. 项目研究与讲解

读取 `references/project-research.md`。把用户明确提供的 GitHub 仓库、本地目录或源码包讲成第一次接触该领域的人也能理解的价值与用途说明。

1. 先根据来源与问题选择研究深度。只提供项目时回答“它为谁解决什么问题、用户最终得到什么、什么时候有用”；用户追问内容、实现、效果、使用、维护、比较或报告时才进入对应深度。
2. GitHub URL 默认读取仓库文件；本地目录和源码包以本地内容为真源。当前维护状态、版本、Stars、Issues 和 Releases 只有用户明确询问时才查询实时官方来源。
3. 源码归档需要解压时，先确定目标根：用户明确指定目录时使用该目录，否则使用 `E:\Work\BaiduSyncdisk\Code\Example`。随后运行 `scripts/extract_project_archive.ps1 -ArchivePath <路径> -DestinationRoot <目标根>`。脚本在目标根下创建独立目录并返回可研究的项目根，不覆盖已有内容。只有脚本成功返回且项目根可读时才继续研究；失败时保留已产生目录，报告准确错误并停止本条路径，不改用其它临时目录继续。
4. 用户询问实际效果、可靠性、上下文噪音、值不值得或是否适合某个目标时，再读取 `references/project-effectiveness-review.md`，区分项目声明、仓库原始产物、作者评测和本次真实复现。
5. 用户明确要求亲自运行或复现时，才安装或启动项目，并从真实入口经过正式处理边界到最终用户结果。用户只问项目用途或现有效果时保持只读。
6. 用户明确要求保存研究文档时，再读取 `references/project-research-report.md`，按当前问题选用章节，不把维护状态、运行验证或完整证据索引自动加入普通介绍。
7. 用户明确要求完整阅读参考项目，并判断哪些能力值得另一个目标项目吸收时，按 `references/project-research.md` 的“完整能力吸收研究”形成来源覆盖、独立行为、运行假设、授权边界、目标缺口与采用候选。只要求建议时保持只读；明确要求计划或实施时，才让“改动前预防”消费这份来源能力台账。

用户只要求看懂项目时，默认第一段让读者能够回答“它解决什么问题、我最终能得到什么、为什么和什么时候会需要它”，达到当前问题的理解结果后停止。只有第 4 项条件成立时，才进一步给出效果、适用性或“值不值得用”的判断；源码目录、框架、CI、测试或版本信息只在会改变当前答案时出现。

### 7. 项目基线与模板

读取 `references/project-template-system.md`。模板把已经稳定成某类项目默认约束的能力变成后续项目会实际读取和验证的合同，同时保留项目自己的业务与产品决定；仍需结合情境判断的方法继续由 Project Steward 主流程负责。

1. 运行 `python scripts/project_templates.py inspect <项目根>`，取得普通文件夹、精确根 Git 能力、技术栈信号、现有项目配置和建议模板。脚本只读取项目。
2. 按 `base → surface → archetype → stack → capability → project overrides` 解析模板。当前注册表位于 `assets/project-templates/catalog.json`；注册只表示可显式选择或被确定性检测，不自动覆盖用户目标。`explicit_only` 能力模板只在用户或项目配置点名时进入。`product-ui` 只在项目有稳定设计上下文、由上层模板依赖或用户显式选择时进入。
3. 新项目先用目标框架的真实脚手架建立源码，再用 `plan` 确认治理模板、默认决策和会新增的项目配置。已有项目先做采用计划，明确哪些现状符合、哪些需要作为偏离保留、哪些需要治理。
4. 用户明确要求采用时运行 `adopt`，只创建 `.project-steward/project.json`；目标已存在时拒绝覆盖。用户明确要求升级时先运行 `plan`，再运行 `upgrade`，保留项目覆盖与偏离理由。
5. 运行 `verify` 让正式消费者读取项目配置、目录和模板检查。确定性检查通过后，继续完成模板列出的人工代码链与真实用户链。

创建、采用、升级和验证是四个独立动作。只要求建议或计划时交付只读结果；只要求创建项目时停在本地项目。

### 8. 模板演进

读取 `references/template-evolution.md`。用户明确要求更新项目模板时，把已经确认应成为默认约束的稳定能力写到最早且最窄的正确位置。普通项目修复完成项目结果后停止；Project Steward 自身的通用方法改进只由“对话学习与自我进化”处理。

进入写入后：

1. 用至少一个可复现缺陷、独立项目事实、正式平台合同、跨项目独立验证结果或用户明确长期决定说明现有模板缺口。
2. 把经验前移到最早决策点，判断它属于 `base`、`surface`、项目形态、技术栈、能力包还是单项目覆盖。
3. 沉淀稳定行为、检测信号、验证方法和必要资源；当前任务的报错文本、补丁细节和项目专名留在本次证据。
4. 递增模板版本和目录哈希，更新直接消费者与测试；旧版本配置通过 `plan` 显示漂移，通过 `upgrade` 一次迁移。
5. 使用一个全新的 Skill 外部临时项目从注册表冷启动，验证识别、采用、读取、检查和升级链。

### 9. 产品体验与界面治理

读取 `references/product-experience-governance.md`，先选择本轮交付与停止位置：定义产品体验、研究参考产品、讨论页面或流程目的、诊断体验问题、比较方向、制定详细方案、统一设计系统、综合体验审计、实际实现审查、实施设计或忠实重建。只读讨论、审查和方案不会自动授权修改项目；实施和忠实重建只在用户明确要求时写入。

全局页面推进、章节内部演出、局部步进、媒体完成或导航恢复中有两个以上共同影响同一段连续体验，或者多个页面反复出现提前播放、页面交界、输入迟钝、反向空滚或回访重放问题时，在调整阈值、时长和单页动画前先冻结体验保证与取舍，并用 `references/interaction-motion.md` 为每个区域选择唯一主架构、阶段生命周期、方向性输入所有权和返回策略。互相制约的保证必须先说明优先级与代价；普通内容流不为统一形式强行增加固定舞台、分页落页或滚轮接管。

按问题直接读取需要的资料，不让 reference 再指挥 reference：

- 判断品牌传播界面、任务型产品界面或混合表面：`references/surface-registers.md`。
- 理解用户与情境、旅程、信息架构、导航、任务流、参考研究或体验验证：`references/ux-design.md`。
- 系统审查已有产品的跨页面、跨状态和完整任务体验：`references/task-experience-audit.md`。
- 设计、审查或修复任务状态、阶段、百分比、聚合进度和无法测量阶段的表达：`references/task-progress-governance.md`。
- 判断和改善界面是否一体、顺手、舒服、合适并保持掌控感：`references/interface-experience-quality.md`。
- 从体验目标确定界面方向、改版模式、页面去留或候选取舍：`references/design-method.md`。
- 处理空间预算、集合、媒体构图、源内容与输出比例、低高度窗口和响应式：`references/layout-responsive.md`。
- 方案依赖外部图片、视频或三维生产工具时，先在 `references/product-experience-governance.md` 建立工具能力、正式素材生产者与派生链合同，再让布局、互动和实施验收消费最终素材身份；文件已经生成不能代替用途、来源和正式消费者成立。
- 制定组件、表单、文案职责、排版、颜色角色和无障碍规格：`references/interface-guidelines.md`。
- 处理输入反馈、命中、可见拖入创建时的放置预览与提交一致性、动效生命周期、滚动、导航恢复、集合抽样消费和排他转场；多页面、长滚动或媒体叙事先建立正式页面与状态顺序以及滚动性能责任合同，再让导航、过场、预热和动效消费这些真源；视频、音频或其它时序媒体还需要明确资源到达、原子切换、完成策略、一次性生命周期、分类播放与声音职责，以及怎样防止旧回调接管当前对象：`references/interaction-motion.md`。
- 统一设计语言、token、共享组件和漂移来源：`references/design-system-alignment.md`。
- 平台或跨平台差异会改变结构、输入、导航或系统行为：`references/platform-guidelines.md`。
- 媒体预览表现为内容缺失、裁切或比例留白且边界不清，或高频、系统性界面问题需要从症状回到稳定根因时：`references/interface-problem-patterns.md`。
- 审查已实现或已运行体验，或实施后验收最终结果：`references/implementation-review.md`。
- 用户明确要求还原、重建、复刻或像素级匹配有权使用的界面：`references/reference-interface-reconstruction.md`，并由主路径为每个待实现区域提供 `assets/reference-reconstruction/component-spec.md`。

设计强度按影响范围决定：普通项目审计只做轻量体验健康检查；会改变可见界面或交互语义的预防与根因治理补充界面合同；新产品、整体改版和完整体验审计进入完整设计；忠实重建只由明确请求触发。没有产品界面的后端、CLI 和库项目跳过本节全部资料。

所有可见界面设计、修改和审查默认做轻量使用质感检查：根据任务、频率、平台和风险确定目标使用感，从自然入口完成一次受影响任务，并检查相邻区域、反馈和最终画面。用户明确说不顺手、不舒服、不合适、别扭、没有整体感或没有质感，或者多个症状指向共同边界时，做完整检查，覆盖相关表面、连续主任务、重复使用、压力状态和共享消费者。局部切换延迟、最终落点或单张截图已经通过但用户仍报告滚动迟钝、空滚或失控时，结论退回体验架构与输入所有权，不能继续只调阈值、冷却时间或动画速度。精确文字替换和用户已经锁定关系的局部修改不因此扩张到整个产品。

界面实施仍遵守项目级唯一真源与完整迁移。共享主题、token、组件、平台外壳或状态投影是根因时，从该来源迁移全部消费者；完成证明必须经过真实入口、正式状态或内容边界和实际视口中的最终画面，不能以源码属性或组件存在代替可见结果。结果需要确认、标记已读或停止重放时，再读取 `references/implementation-review.md`，只有当前活动表面真实可见、非离屏且具有非零几何后才确认。

按需使用外部与运行能力：只有参考研究确实会改变方案或用户明确要求时才查少量当前来源；忠实重建使用真实浏览器检查用户点名且有权使用的页面；用户要求审查实际体验或实施后的可见验收时才启动或控制界面，并只停止本次启动的进程。图片只在用户明确要求图像产物或它是已授权实现不可缺少的资源时生成。设计请求不自动授权登录、购买、复制受限资产、安装依赖、删除、打包、部署或发布。

### 10. 仓库建立与发布

读取 `references/repository-publication.md`。初始化本地 Git、修改治理文件、创建提交、创建远程仓库、推送和改变可见性是独立权限。用户要求另一台机器或服务器从仓库复现结果时，以干净克隆合同确定必须跟踪的构建、运行和仓库能力输入；当前差异、跟踪状态或忽略规则都不能单独决定仓库边界。提交前审计 Git 实际暂存索引，并把最终候选中的每类变化映射到正式消费者和最后一次相关变化后的新鲜证据；可见界面、媒体或交互变化需要相应真实用户链，类型检查或构建成功不能代替。证明每条检查命令成功后才继续；远端不存在时，推送请求不自动授权创建仓库或替用户选择可见性。

只执行用户明确要求的动作。发布后核对真实默认分支、远程 HEAD、关键 Git blob、GitHub 首页和许可证识别；当前用户结果依赖远端检查或部署时继续核对相应状态。读取失败结果属于验证，修改项目修复失败仍需原请求已经授权持续修复或用户另行确认；不能把本地预览说成已经发布，也不能用强制推送掩盖分叉。

### 11. README 与主页

先选择互斥子类型：

- **只读审计**：评价现有阅读路径，不写文件。
- **README 整页模式**：用户明确要求重写、重组或整体改善。
- **仅素材模式**：只制作 hero、章节标题、图、徽章、展示板或动画；未获授权时不改 README。
- **Star History 自动化**：用户询问 README 星标历史图失效、替代、接入或长期维护时，直接读取 `references/github-star-history.md`。只问原因、现状或复杂度时检查第三方来源、Actions、工作流和输出分支并保持只读；明确要求实施时同时读取 `references/repository-publication.md`，使用 Project Steward 的中央生成器和调用仓库短期 `github.token`，完成调用工作流、独立输出分支和 README 消费端迁移，精确提交并推送调用仓库改动，手动运行工作流，不复制生成器，不保留旧第三方图片回退。最后沿“stargazers API → 生成器 → 输出分支 → raw SVG → README”真实链路验收。

只读审计和整页模式先建立公开事实与材料资格，再建立公开读者合同。当前用户明确要求决定公开目标；当前活动的核心定义、源码、配置、真实入口和运行结果证明对应项目状态下的现状。现有 README、专项文档、示例、内部图片、截图、流程图和生成物只作为候选材料：使用前必须能对应到产生它的项目状态、它表达的事实或关系、当前活动真源和预定公开职责。用户已明确指出过时、无法确定适用版本、与当前真源冲突或无法完成这种对应的材料，标为过时或未验证，退出公开事实、证据池、视觉证据等级和复用候选；文件能够读取、图片能够渲染或审计脚本通过都不能改变这项资格。

可复用 Skill、插件和方法包的项目身份由活动核心工作流、实际依赖和真实使用方式决定。`agents/openai.yaml` 等宿主元数据只证明相应适配、展示或分发入口存在，不能单独把通用项目缩窄为该宿主专用。只有宿主专属能力确实改变可用入口、输入、动作或结果时，才把宿主写入兼容、安装、限制或项目身份。项目身份、宿主角色和材料资格在本层确定一次；正文与视觉资源只消费，不重新选择。

随后使用已经核定的当前项目事实建立公开读者合同：

```text
第一采用读者：最可能判断“这是否适合我”的人，以及他实际需要的专业程度
首页读者变化：读者看完首屏与首个核心章节后，新增哪项理解、形成哪项判断或知道怎样开始
项目本体与运行载体：仓库实际交付什么，谁会加载或使用它，当前宿主是否只是兼容与安装信息
项目身份真源与宿主角色：哪些活动事实定义项目，宿主信息实际改变什么
到访情境：他遇到了什么、想完成什么
交给项目什么：一句请求、文件、链接、数据、命令或其它真实输入
可观察结果：使用后能看到、拿到或完成什么
第一步：无需先理解内部实现就能采取的下一行动
后续读者：安装者、开发者、维护者和贡献者需要到哪里继续
场景证据：前段每个使用场景分别对应哪条活动入口、真实输入、交付和停止位置
公开事实与合格材料：哪些当前事实和材料可以进入正文或视觉，哪些已退出及原因
```

用户明确指定读者时以该读者为第一层。项目真实入口、输入、输出和停止位置共同确定项目本体。主页直接使用已经核定的项目身份；宿主差异只在上层已经证明会改变安装或使用方式时进入相应说明。

公开首层先兑现一个首页读者变化，再按“项目身份 → 当前支持的主要工作流 → 直接入口”组织。首页读者变化必须能由第一采用读者用自己的话复述为新增的理解、判断或行动。动作差异写成“条件 → 动作 → 交付 → 停止位置”，权限与停止位置在对应操作步骤出现一次。

项目同时面向普通使用者和技术维护者时，首屏、首个核心章节和第一次成功路径服务普通采用者，安装原理、内部机制、目录、脚本和维护资料进入后续层。第一次真实使用本身要求技术知识时，第一层使用相应技术语言并说明当前用途。仓库内部分类、路由名、合同字段和实现术语先作为事实与证据，再按第一采用读者的行动价值决定位置。前段使用场景逐项对应当前活动入口、真实输入、交付和停止位置。

随后读取 `references/content-architecture.md`，让它消费已经确定的项目身份、材料资格和公开读者合同，把首页读者变化展开为各章节的读者变化、材料预算、正文结构和回读标准；它不重新选择第一采用读者，不重新认定宿主角色或候选材料资格。

整页模式及表达性视觉任务在制作素材前建立视觉证据合同：

```text
项目自身视觉体系：由产品界面、品牌资料、来源独立且一致的资产或用户确认的规则共同证明
项目原生材料：已经通过来源与时效核定的真实界面、输出、处理对象、关系、代码表达或文档语气
不采用材料：过时、版本不明、与当前真源冲突或职责无法成立的候选及原因
家族视觉合同：项目明确属于哪个家族，以及只允许共享哪些颜色、字体、标志或其它元素
证据等级：已有体系 / 原生材料充分 / 证据不足
```

当前 README 视觉、仓库内部图片和同批次生成的素材无论数量多少都只作为待评估产物；只有已经通过上层来源与时效核定的材料才能进入视觉合同。未核定材料不能因为位于仓库、链接有效、画面完整或曾在 README 出现就成为事实证据或视觉依据。没有独立产品表面、品牌资料、可追溯设计规则或用户确认时，它们不能证明项目已有视觉体系。没有明确家族合同的项目按独立项目处理；仓库类型、技术栈、同属某一种 Agent Skill 或使用同一宿主不能代替家族合同。

在同一上层按证据等级选择一次视觉路径：

- **已有体系**：延续已被多项证据证明的项目视觉语言，家族元素只共享合同明确列出的部分。
- **原生材料充分**：读取 `references/visual-direction.md`，从项目自身材料形成彼此明显不同的方向并用项目证据选定一个。
- **证据不足**：整页模式可以不制作表达性素材；用户明确要 hero 或标题素材时只制作项目事实支持的排版型素材，不补造技术卡片、抽象流程或风格故事。

整页模式必须明确选择复用已有、制作新的、排版表达或跳过，并说明这个选择怎样服务首页读者变化；不能把仓库中碰巧已有图片当成已经完成视觉设计。选定路径后，`references/visual-direction.md` 只把上层合格材料与证据合同转换成颜色、字体角色、构图、形状、主题元素和密度，不重新选择证据等级或恢复已退出材料。hero 随后读取 `references/project-native-hero.md`；所有实际视觉产物读取 `references/github-readme-canvas.md`。制作 SVG 时再读取 `references/svg-production.md`；用户选择混合 SVG 与栅格构图时读取 `references/hybrid-svg-production.md`；只有用户明确选择 GIF 时读取 `references/motion-production.md`。下层资源完成各自职责后返回当前 README 子类型。

整页修改后运行 `python scripts/audit_readme.py <README路径>`，并在 GitHub 常见桌面宽度、窄屏和深浅背景检查最终页面。该脚本只证明图片引用、替代文字和基础 SVG 结构，不证明材料仍然有效、事实准确、视觉适合当前读者或最终页面质量。GIF 使用 `python scripts/render_motion_gif.py <输入SVG> <输出GIF> --spec <规格JSON>`。视觉解释机制时不能冒充运行结果，主要承诺必须邻近真实证据。

### 12. 许可证治理

先选择互斥子类型：

- **建议模式**：许可证选择、差异解释或现状审计；默认只读。
- **单项目应用模式**：准确范围和方案已经确认后写入一个项目。
- **账号分配模式**：为一个 GitHub 账号范围生成完整逐仓库矩阵；默认只读。
- **批准后的批量实施模式**：用户批准准确矩阵后逐仓库实施。

所有许可证任务读取 `references/license-governance.md`。本地项目运行 `python scripts/inventory_repository.py local <项目路径>`；GitHub 范围运行 `python scripts/inventory_repository.py github --owner <账号> [--repository <仓库名>]`。盘点只提供证据，不替 Agent 选择许可证。

写入前读取 `references/license-rollout.md`，从 `assets/licenses/catalog.json` 取得正式文本和哈希，生成 schema v2 方案。先运行 `python scripts/apply_license.py --plan <方案JSON>` 全量预演；本地目标用 `--write`，远程目标用 `--publish`，完成后用 `--verify` 读取最终状态。替换已有许可证必须逐字节保留历史文本；不能通过新根许可证撤销旧版本权利或覆盖第三方条款。

### 13. 人性化日志

先选择互斥子类型：

- **日志理解与审查**：从一次真实操作出发，检查人能否看懂发生了什么、关键输入输出是什么、问题停在哪里以及最后得到什么；默认只读。
- **日志改造**：用户明确要求修复、优化或重构时，让摘要、完整原文、关联事件和最终结果在正式查看入口中都可用。

两类都读取 `references/log-audit-standard.md`，建立 `运行事件 → 日志调用 → 上下文字段 → 格式化与过滤 → 写入与轮转 → 读取与展示` 地图。

第一段先用当前项目中的人和动作说明一次关键过程能否被还原，再解释缺失信息最早发生在哪里。日志改造在上下文、写入、存储和展示的唯一边界收口，迁移全部调用点并退出旧数据源。LLM 请求、回复和工具调用保留完整可查看原文，凭据在产生记录的边界单独处理；正式日志生产者输出必须被真实存储和消费者读取，并能在界面、文件或导出结果中查看。跨进程或异步关闭日志分别保留发起者、终止原因、结果状态、传输收尾、退出码和清理结果，不能让 EOF 或后到的通用状态覆盖结构化原因。

### 14. 用户环境档案与执行环境

读取 `references/user-environment-governance.md`。用户明确要求建立、检查、更新或解释当前用户环境时，按 `inspect → plan → apply --write → verify → resolve` 的边界工作；查看和计划保持只读，写入环境档案不自动授权安装、下载、修改系统环境变量或删除工具。

环境档案默认位于 Codex 用户目录下的 `project-steward/environment-profile.json`，schema 由 `assets/user-environment/profile.schema.json` 负责，确定性操作由 `scripts/user_environment_profile.py` 负责。档案只保存经过允许的用户偏好、机器检测事实、非敏感运行要求和核验信息，不保存凭据、完整环境变量、进程快照或项目依赖清单。

其它主路径只有在工具位置、包或缓存根、终端、SDK 或长任务状态会改变本次动作与验收时，才读取并消费相关条目。发生长任务超时时，先区分外层等待超时、构建工具失败和原进程仍在运行；检查阶段、进程、日志、产物与正式消费者后再决定继续观察、停止或重试，不能在原任务仍有进展时启动重复任务。

完成写入后必须由 `verify` 读取同一份档案，再由 `resolve` 返回本次需要的绝对可执行文件和必要环境变量，并让真实命令消费；工具需要 Rust 等环境变量时，版本核验也必须在解析出的同一环境中运行。

### 15. 项目综合审计

用户明确要求全面检查、健康判断、风险排序或综合审计时读取 `references/project-audit.md`。该路径保持只读；用户要求“全部”“所有”或“全面”时，先冻结实际可覆盖的项目边界、用户结果、运行入口、成功与失败路径、构建或发布层以及未取得的证据，完整性结论只对这份覆盖合同成立。覆盖：

- 产品目标、项目边界和内容真源是否一致；
- 架构生产链、持久化边界和平台能力是否清楚；
- 当前项目模板、实现和文档是否漂移；
- 项目长期决定是否位于真实开发流程会读取的规则、文档、配置、代码或测试中；
- 构建、测试、发布和真实用户链是否有可信证据；
- 真实产品界面存在时，对核心任务、布局所有权、状态完整性、响应式和设计系统做轻量健康检查；
- README、许可证与日志专项是否存在会影响当前项目的明显缺口。

第一段说明项目目前是否健康、最重要的根因级风险和第一优先事项。之后按证据强度给出项目事实、问题影响、建议路径和仍缺的证据，交付后停在只读结论。

用户明确把高内聚低耦合、重复实现或上帝模块作为综合审计重点时，同时读取 `references/architecture-cohesion-governance.md`，让专项方法提供职责、依赖、真源和语义重复证据；综合审计负责把这些证据放回项目级风险顺序。

## 平台模板资源

选择 `product-ui` 模板，或当前改动会影响真实产品界面时，按“产品体验与界面治理”选择必要资料。模板固定用户任务、界面合同、界面使用质感、设计系统真源和真实可见验收，不固定审美风格、组件库、尺寸或当前框架 API。

选择 `local-file-workspace` 模板，或项目把用户可见的本地文件夹作为核心工作区时，读取 `references/local-file-workspace-governance.md`。它规定磁盘文件真源、普通文件夹首要路径、可选版本管理、目录树、文件操作、可恢复删除和源文件保真，不把 Git 或其它高级能力变成打开文件夹的前提。

选择 `desktop-app` 模板，或当前问题涉及原生桌面窗口、生命周期、系统集成和本地持久化时，读取 `references/desktop-app-governance.md`。它规定窗口外壳、关闭语义、刷盘、原生权限和视觉验收的共同边界。

桌面窗口会在紧凑栏、工作区、悬浮控制条或其它呈现状态之间切换时，由该资料进一步建立窗口呈现状态矩阵，区分外框与内容区尺寸、系统标题栏与按钮保留区、拖动命中、用户位置和跨显示器缩放。屏幕捕获产品还由它治理每个自有顶层窗口是否进入录制，并要求从真实录制产物验证最终结果。

桌面应用存在辅助进程、管道或事件流时，同一资料还要求从进程创建前建立所有权，分开业务结果、诊断日志、终止原因、进程结果和传输收尾，等待权威结果与终止事件交付、日志写入和订阅者排空，明确标准流编码，并用有界零退出码判断正常关闭。

选择 `tauri-vue` 模板，或项目事实确认使用 Tauri + Vue 时，再读取 `references/tauri-vue-governance.md`。它负责 Tauri 配置、Vue 外壳、拖动区、窗口按钮、权限、关闭链和 Windows 实机验证，不改变上层主路径或用户授权。

这些平台资源只补充已经选择的项目路径；它们不能把普通项目任务改选成桌面应用任务。

## 输出与完成

第一段直接给当前结果：

- 对话学习与自我进化按多个用户最终结果说明已证明有效的机制、缺失或错误机制、最早预防点和适用范围；发生写入时说明 Project Steward 哪项活动能力改变、未来怎样触发和验证。
- 改动前预防说明实际采用、调整或排除的 Project Steward 方法与项目规则，随后说明唯一边界、受影响链、旧路径退出条件和完成证据。
- 根因治理说明错误最早产生在哪里、最终边界怎样收口、旧架构是否已经退出，以及真实用户链是否成立；疑难诊断还说明竞争解释如何被证据排除、哪些特殊维度实际改变了动作，以及昂贵完整链为何只承担最终确认；连续收口还说明每轮由什么新证据推进、为何在当前证据点停止；架构专项还直接回答内聚、耦合、语义重复和上帝模块。
- 项目研究第一段说明项目为谁解决什么问题、用户最终得到什么、为什么和什么时候会需要它；用户追问的内容随后按当前深度回答。能力吸收研究还要说明来源覆盖、独立行为、目标缺口、采用或拒绝理由，以及当前停在只读建议还是已经交给改动前预防。
- 项目基线说明哪些稳定治理能力通过哪些模板进入项目、配置写到哪里以及验证停在哪一层。
- 模板演进说明哪项稳定能力进入哪个模板、版本怎样变化，以及外部冷启动链是否通过。
- 产品体验与界面治理说明用户与任务、目标体验与目标使用感、设计方向或根因、保护区、实现状态，以及界面是否一体、顺手、舒服、合适并通过真实可见与连续操作验收。
- 用户环境档案与执行环境说明档案位置、推荐终端、关键工具与包根、检测到的漂移、实际消费证据和仍未核验的能力。
- README、日志、仓库发布和许可证分别说明读者、操作者或维护者最终能看到、理解或使用的结果。
- 综合审计说明项目是否健康、最重要风险和第一优先事项。

只读任务交付证据与建议后停止。写入任务列出准确文件、可观察变化和已经运行的验证。结构可达、预演、真实实例化、真实运行、本地提交和远程发布必须明确区分；用户未授权提交或发布时，结果保持在本地项目。
