name: Update Star History

on:
  schedule:
    - cron: "17 3 * * 1"
  workflow_dispatch:

permissions:
  contents: write

jobs:
  update:
    permissions:
      contents: write
    uses: ./.github/workflows/star-history.yml
